<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Driver Confirmation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
        h2 {
            text-align: center;
        }
        .btn-container {
            text-align: center;
            margin-top: 20px;
        }
        .btn-container button {
            padding: 10px 20px;
            margin: 0 10px;
            border: none;
            border-radius: 3px;
            background-color: #dc3545;
            color: #fff;
            cursor: pointer;
        }
        .btn-container button:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Delete Driver Confirmation</h2>
        <p>Are you sure you want to delete this driver?</p>
        <div class="btn-container">
            <button id="confirmBtn">Yes, Delete</button>
            <button id="cancelBtn">Cancel</button>
        </div>
    </div>

    <script>
        document.getElementById("confirmBtn").addEventListener("click", function() {
            // Redirect to the delete action
            window.location.href = "/Home/DeleteDriver";
        });

        document.getElementById("cancelBtn").addEventListener("click", function() {
            // Redirect back to the drivers page or any other page
            window.location.href = "/Home/Drivers";
        });
    </script>
</body>
</html>
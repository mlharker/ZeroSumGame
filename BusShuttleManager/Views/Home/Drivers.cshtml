@model IEnumerable<BusShuttleManager.Models.DriverViewModel>
@{
    ViewData["Title"] = "Drivers";
}

<style>
        /* Add your CSS styles here */
        /* Example styles for navigation bar */
        .navbar {
            overflow: hidden;
            background-color: #333;
        }

        .navbar a {
            float: left;
            display: block;
            color: white;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
        }

        .navbar a:hover {
            background-color: #ddd;
            color: black;
        }
    </style>

<div class="navbar">
    <a href="/">Drivers</a>
    <a href="/Busses">Busses</a>
    <a href="/Routes">Routes</a>
    <a href="/Stops">Stops</a>
    <a href="/Loops">Loops</a>
</div>

<h2>Drivers</h2>
<a href="/CreateDriver">Add new driver</a>

<table class="dataTable">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.FirstName)</th>
            <th>@Html.DisplayNameFor(model => model.LastName)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                <td>@Html.DisplayFor(modelItem => item.LastName)</td>
                <td>
                    <a asp-action="Edit" asp-Routes-id="@item.Id">Edit</a> | 
                    <a asp-action="Delete" asp-Routes-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
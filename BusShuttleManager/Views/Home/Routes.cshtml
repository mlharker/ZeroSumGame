@model IEnumerable<BusShuttleManager.Models.RoutesViewModel>
@{
    ViewData["Title"] = "Routess";
}

<style>
        /* Add your CSS styles here */
        /* Example styles for navigation bar */
        .navbar {
            overflow: hidden;
            background-color: #333;
        }

        .navbar a {
            float: left;
            display: block;
            color: white;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
        }

        .navbar a:hover {
            background-color: #ddd;
            color: black;
        }
    </style>

<div class="navbar">
    <a href="/">Drivers</a>
    <a href="/Busses">Busses</a>
    <a href="/Routes">Routes</a>
    <a href="/Stops">Stops</a>
    <a href="/Loops">Loops</a>
</div>

<h2>Routess</h2>
<button asp-action="Add Routes">Create new Routes</button>

<table class="dataTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayNameFor(modelItem => item.Order)
                </td>
                <td>
                    <button asp-action="Edit" asp-Routes-id="@item.Id">Edit</button> | <button asp-action="Delete" 
                    asp-Routes-id="@item.Id">Delete</button>
                </td>
            </tr>
        }
    </tbody>
</table>